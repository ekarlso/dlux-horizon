language: python
python:
  - "2.6"
  - "2.7"
cache: 
  - pip
install: 
  - "pip install -r requirements.txt -r test-requirements.txt"
  - "apt-get -qq install firefox"
script: 
  - tox -e pep8
  - "./run_tests.sh -c -V --with-selenium"
notifications:
  irc:
    - "chat.freenode.net#opendaylight-dlux"
